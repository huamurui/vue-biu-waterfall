// here we will push the computed styles to the cells
// is it possible to create a space here to store the adaptedRect and computed styles and then push them to the cells?
// no... get the computation and operations back to the WaterfallConfig
export {

}